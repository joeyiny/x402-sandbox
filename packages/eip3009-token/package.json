{
  "name": "@x402-sandbox/eip3009-token",
  "version": "0.1.0",
  "description": "EIP-3009 compatible token for x402 testing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "compile": "hardhat compile",
    "build": "echo 'No TypeScript files to build'",
    "test": "hardhat test",
    "typecheck": "echo 'No TypeScript files to check'"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}
